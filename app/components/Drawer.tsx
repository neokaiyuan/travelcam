const drawerStyle = {
  backgroundColor: "var(--navbar-drawer-bg)",
  color: "var(--foreground)",
  // Add any other styles that match the navbar
};

function Drawer() {
  return <div style={drawerStyle}>{/* Drawer content */}</div>;
}

export default Drawer;
